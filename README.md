
  # PDF Previewer UI

  This is a code bundle for PDF Previewer UI. The original project is available at https://www.figma.com/design/GsP5OD6eeRskUPVHa6uutV/PDF-Previewer-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  